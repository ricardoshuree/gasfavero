[project]
name = "mcp-local-erp"
version = "0.1.0"
description = "Servidor MCP local de leitura e escrita para um projeto ERP"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0",
    "pyyaml>=6.0",
    "psutil>=6.0",
]

[tool.uv]
package = false
